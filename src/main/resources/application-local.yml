server:
  port: 8090
  servlet:
    context-path: /

spring:
  datasource:
    # Local Host Connect (Docker Port Mapping 33306 -> 3306)
    url: jdbc:mariadb://localhost:33306/db_nanum?serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: ttcc1234
    driver-class-name: org.mariadb.jdbc.Driver

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.ctso.domain.**.model
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: INFO
    com.ctso: DEBUG

file:
  storage:
    type: local
    local:
      path: ${CONTAINER_UPLOAD_PATH:./uploads/}
